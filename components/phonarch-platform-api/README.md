# Phonarch Platform API

Authenticated Go control plane for workspaces, conference rooms, participants,
batch dialing, speaker requests, node telemetry, and durable PostgreSQL state.

Build output is written to `bin/` by `../../deploy/scripts/build.sh`. Database
migrations are kept beside this component under `migrations/`.
