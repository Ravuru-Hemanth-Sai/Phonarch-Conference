# Phonarch Platform Web

Next.js App Router console for workspace landing, conference-room operations,
direct and bulk dialing, moderated speaker requests, and participant controls.
The application source follows the platform convention under `src/app/`.

The browser is an HTTP administration client only. It does not terminate SIP,
RTP, WebRTC, or WSS media.
